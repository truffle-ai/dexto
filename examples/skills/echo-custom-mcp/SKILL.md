---
description: Use bundled skill files for quick skill bundle checks.
---

# Echo Custom MCP

Use this skill when you need to verify that a skill bundle can bring along inert bundled files.

Workflow:
1. Read the bundled usage reference when more detail is needed.
2. Inspect bundled scripts or MCP config examples as plain files if relevant.
3. Return a concise summary of the bundled file contents you used.

The `mcps/` and `scripts/` files are bundled files only. Skills do not register or connect MCP servers from them.
