---
"@dexto/core": patch
"@dexto/agent-management": patch
"@dexto/tui": patch
"dexto": patch
---

Add local model auth profiles, wire host auth resolution through core sessions, and expose `dexto connect` for OpenAI API keys and ChatGPT Login.
