---
'@dexto/core': patch
dexto: patch
---

Allow hosts to override telemetry bootstrap during agent startup while preserving the default Node bootstrap path, and add the supporting hosted-telemetry registration seam in core.
