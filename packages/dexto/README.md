# Dexto CLI

This npm package is a small wrapper that launches the prebuilt Dexto binary for your platform.

If installation fails due to missing optional dependencies, ensure optional dependencies are enabled in your package manager.

